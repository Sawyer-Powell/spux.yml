---
space: "spux"
root: "/home/sawyer/dev/personal/spux"
windows:
  - name: "shell"
  - name: "vi"
    init: "nvim . {enter}"
    cmds:
      - cmd: "{clear} go build spux.go {enter}"
        tgt: "zsh"
      - cmd: "{clear} go test ./gen {enter}"
        tgt: "zsh"

